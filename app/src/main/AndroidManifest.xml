<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.dia">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Dia"
        android:usesCleartextTraffic="true"
        tools:replace="androidx.appcompat.app.AppCompatActivity">

        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
                <data
                    android:host="oauth"
                    android:scheme="{8a670e86ec1997daaa82ca5c8f08955c}"/>
            </intent-filter>
        </activity>
        <activity android:name=".RegisterActivity"/>
        <activity android:name=".LoginActivity"/>
        <activity android:name=".HomeActivity"/>
        <activity android:name=".SettingActivity"/>


        <!--<activity android:name=".MainActivity">-->
        <!--android:theme="@style/Theme.Daycheck">-->
        <!--<activity android:name=".DayActivity"></activity>-->
        <!-- 카카오 로그인-
        <meta-data
            android:name="com.kakao.sdk.AppKey"
            android:value="8a670e86ec1997daaa82ca5c8f08955c"/> -->
    </application>

</manifest>